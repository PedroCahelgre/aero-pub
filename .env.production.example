# Exemplo de variáveis de ambiente para produção no Netlify

# URL de conexão para o banco de dados PostgreSQL de produção.
# Substitua pelo valor fornecido pelo seu provedor de banco de dados (ex: Neon, Supabase, PlanetScale).
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

# A NEXTAUTH_SECRET é usada para assinar os tokens de sessão.
# Gere uma chave secreta forte usando: openssl rand -base64 32
NEXTAUTH_SECRET="SUA_CHAVE_SECRETA_AQUI"
NEXTAUTH_URL="https://SEU_DOMINIO_NO_NETLIFY.netlify.app"
